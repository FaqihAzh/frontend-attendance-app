name: attendanceapp
description: A professional Flutter attendance application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  get: ^4.6.6

  # Local Storage - updated version
  shared_preferences: ^2.2.2

  # Location Services - updated version
  geolocator: ^10.1.0
  permission_handler: ^11.0.1

  # Utilities
  intl: ^0.18.1

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true